using System.Collections; using System.Collections.Generic; using UnityEngine;   public class scri : MonoBehaviour { 	//Declaring variables 	bool enter; 	public string hintMessage; 	Rect rect; 	Color color;   	void OnTriggerEnter(Collider other)  	{  		//Telling game to actiavte boolean  		enter = true;  	}   	void OnTriggerExit() 	{ 		enter = false;  	}    	void OnGUI ()    	{  		GUI.skin.label.fontSize = 20; 		var centeredStyle = GUI.skin.GetStyle("Label"); 		centeredStyle.alignment = TextAnchor.UpperCenter;    		//If the boolean is active, display the text 		if (enter == true)  		{  			//GUI.Label(new Rect(100, 590, 800, 90), hintMessage, centeredStyle); 			GUI.Label(new Rect(0, 500, 1100, 90), hintMessage, centeredStyle); 			Debug.Log (hintMessage); 			Debug.Log ("Works"); 		}   	} }